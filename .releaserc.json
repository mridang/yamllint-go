{
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "echo '${nextRelease.version}' > cmd/gofmt/VERSION && echo '${nextRelease.version}' > cmd/shfmt/VERSION && echo '${nextRelease.version}' > cmd/tffmt/VERSION && echo '${nextRelease.version}' > cmd/cuefmt/VERSION && echo '${nextRelease.version}' > cmd/protofmt/VERSION && make build"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "build/*.wasm"
          }
        ]
      }
    ],
    [
      "@semantic-release/git",
      {
        "message": "chore(release): ${nextRelease.version} [skip ci]\\n\\n${nextRelease.notes}",
        "assets": [
          "cmd/gofmt/VERSION",
          "cmd/shfmt/VERSION",
          "cmd/tffmt/VERSION",
          "cmd/cuefmt/VERSION",
          "cmd/protofmt/VERSION"
        ]
      }
    ]
  ]
}
